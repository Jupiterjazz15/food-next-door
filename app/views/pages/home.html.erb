<div class="container my-3">
  <!-- Category Cards -->
  <div class="category-cards-container mb-2">
    <%= render "shared/card_subcategory" %>
  </div>
  <p class="my-3"><strong>Food from Effra Court's neighbours</strong></p>
  <!-- Item Cards -->
  <div class="item-cards-grid">
    <%= render "shared/card_item" %>
  </div>
  <p class="my-3"><strong>Food from people living nearby</strong></p>
  <!-- Item Cards -->
  <div class="item-cards-grid">
    <%= render "shared/card_item" %>
  </div>
</div>
