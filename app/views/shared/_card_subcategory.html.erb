<% subcategories.each do |subcategory| %>
  <div class="card-category">
    <%= cl_image_tag subcategory.photo.key, class: "card-category-img"%>
    <div class="card-category-infos">
      <h2> <%= subcategory.name %> </h2>
    </div>
  </div>
<% end %>
